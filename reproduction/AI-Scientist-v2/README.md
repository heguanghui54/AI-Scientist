# AI Scientist-v2 Reproduction Notes

This branch captures the low-cost reproduction work for `SakanaAI/AI-Scientist-v2`.

## What is included

- A compact patch of the local reproduction changes in `ff5897a`.
- The low-cost experiment configuration used for the remote Ubuntu run.
- Prompt updates that bias the run toward a lightweight digit-classification paper with a multi-dataset evaluation path.

## Current status

- Remote Ubuntu experiment is still running.
- The current stage is `stage 2`.
- No final PDF artifact has been produced yet.

## Local commit

- `ff5897a` - `Tune AI Scientist-v2 repro for low-cost runs`

## Notes

The patch file in this branch is intended to preserve the key changes used for the reproduction attempt while keeping the branch small and easy to inspect.
